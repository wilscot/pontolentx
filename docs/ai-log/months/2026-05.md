# 2026-05

## Entries

<a id="2026-05-12-disable-individual-punches"></a>
## 2026-05-12 | Desativacao individual de batidas do dia

Status: approved
Kind: feature, backend, frontend, scheduler

### What Changed
- Added an authenticated `PATCH /api/schedule/<entry_id>/active` endpoint to disable or reactivate individual punch entries.
- Persisted disabled punches with the existing `ignorado` status and added a DB helper to reactivate them back to `pendente`.
- Updated the dashboard card render paths, both Jinja and JS refresh, with `Desativar` and `Reativar` actions.
- Cancelled the scheduler job when a pending punch is disabled and restored scheduling when a disabled future punch is reactivated.
- Marked `docs/A FAZER/01-desativar-batidas-do-dia.md` as successfully executed for the next implementation agent.

### Why
- The user needs to handle partial workdays or custom daily flows without turning the whole day into folga, feriado, facultativo, or meio expediente.

### Files
- app.py
- db.py
- templates/index.html
- static/style.css
- docs/A FAZER/01-desativar-batidas-do-dia.md

### Verification
- `python -m py_compile app.py db.py scheduler.py punch.py holidays.py browser_profiles.py`
- Flask temp-db route checks for disable, reactivate, registered-punch rejection, and past-day rejection.
- Flask temp-db dashboard render and `/api/week` status check.
- `git diff --check -- app.py db.py templates/index.html static/style.css`

### Open Edges
- Browser visual validation through Playwright was attempted but blocked because local Playwright could not find Chrome at the expected path.
- Step 02 still needs to adapt jornada/almoço calculations to respect ignored punches.

<a id="2026-05-12-workday-lunch-recalculation"></a>
## 2026-05-12 | Regras de almoço, jornada e recálculo diário

Status: approved
Kind: feature, backend, frontend, scheduler

### What Changed
- Added scheduler rules to generate lunch between `1h00` and `1h15`.
- Added automatic daily work generation between `7h45` and `8h15` for full days and direct `entrada` -> `saida` days.
- Recalculated future pending punches for the same day after a real registered punch changes the expected daily total.
- Preserved `registrado`, `ignorado`, and `manual_override=1` entries during automatic recalculation.
- Recalculated the day when punches are disabled or reactivated so ignored lunch entries affect the exit time immediately.
- Added dashboard reconciliation for the current day before rendering cards, covering cases where a previous real punch did not trigger recalculation at registration time.
- Added frontend auto-refresh every 60 seconds and on tab visibility return so visible cards reflect recurring recalculations.
- Added manual-edit confirmation when the predicted daily work total would fall below `8h00`; cancel keeps the original time, confirm resubmits with `force: true`.
- Updated setup copy and marked `docs/A FAZER/02-regras-jornada-e-almoco.md` as executed.

### Why
- The user needs automatically generated and adjusted schedules to keep lunch and total workday within business limits while respecting individually ignored punches from step 01.

### Files
- app.py
- db.py
- scheduler.py
- templates/index.html
- templates/setup.html
- docs/A FAZER/02-regras-jornada-e-almoco.md

### Verification
- `python -m py_compile app.py db.py scheduler.py punch.py holidays.py browser_profiles.py`
- Python temp-db checks for normal generation with lunch `1h00..1h15` and workday `7h45..8h15`.
- Python temp-db check for `pausa` and `retorno` ignored, using direct `entrada` -> `saida` workday calculation.
- Python temp-db check for delayed real punch recalculating a future pending `saida`.
- Flask temp-db checks for manual edit confirmation, cancel without save, and forced save.
- Python temp-db reproduction of `entrada 07:39`, `pausa 11:38`, `retorno real 13:03`, `saida 16:31`, reconciling to the valid exit window.
- `git diff --check -- app.py db.py scheduler.py templates/index.html templates/setup.html docs/A FAZER/02-regras-jornada-e-almoco.md`

### Open Edges
- Browser visual validation through Playwright remains blocked because local Playwright cannot find Chrome at the expected path.

<a id="2026-05-15-manual-bank-balance"></a>
## 2026-05-15 | Banco de horas manual oficial

Status: approved
Kind: feature, backend, frontend

### What Changed
- Added the persistent `manual_bank_minutes` config value, stored as integer minutes in the existing config table.
- Added a `/setup` field for the official bank-hours balance, accepting `HH:MM`, `+HH:MM`, `-HH:MM`, and blank as zero.
- Rejected invalid balance formats without partially saving other setup fields.
- Changed the dashboard "Banco de horas" metric to use the manual official balance and keep daily card balances calculated from real punches.
- Marked `docs/A FAZER/03-banco-de-horas-manual.md` as executed.

### Why
- The user needs the summary bank-hours card to match the official company app instead of deriving the total from the local displayed schedule window.

### Files
- app.py
- db.py
- templates/setup.html
- docs/A FAZER/03-banco-de-horas-manual.md

### Verification
- `python -m py_compile app.py db.py scheduler.py punch.py holidays.py browser_profiles.py`
- Flask temp-db checks for saving `+02:30`, `-01:15`, and blank balance, validating persistence and dashboard value/hint/accent.
- Flask temp-db checks for rejecting `2,30`, `abc`, and `01:99` without partial save.
- `git diff --check -- app.py db.py templates/setup.html docs/A FAZER/03-banco-de-horas-manual.md`

### Open Edges
- Browser visual validation was not run for this step.
